[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "epilepsy-microglia"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["anndata>=0.11,<0.12", "numpy", "pandas", "scipy"]

[tool.setuptools.packages.find]
where = ["src"]
